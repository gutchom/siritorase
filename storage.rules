rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /picture/{pictureId=**} {
      allow get, create;
    }
    match /ogp/{pictureId=**} {
      allow get, create;
    }
  }
}
